# Password required to log into /admin.
ADMIN_PASSWORD=

# Auto-populated by Vercel when a Blob store is linked to this project
# (Storage tab in the Vercel dashboard, or `vercel blob create-store`).
# Needed for the admin panel to persist image choices and uploads.
BLOB_READ_WRITE_TOKEN=
